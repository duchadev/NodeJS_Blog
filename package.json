{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "lint-stage": "prettier --single-quote --trailing-comma all --tab-width 4 --write 'src/**/*.{js,json,scss}",
  "husky":{
    "hooks":{
      "pre-commit": "lint-staged",
      "pre-push": "npm test",
      "..." : "..."
    }
  },
  "scripts": {
    "beautiful": "lint-staged",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --inspect src/index.js",
    "watch": "node-sass --watch src/resource/scss/ -o src/public/css/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "express-handlebars": "^7.1.2",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "morgan": "^1.10.0",
    "node-sass": "^9.0.0",
    "prettier": "^3.2.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
